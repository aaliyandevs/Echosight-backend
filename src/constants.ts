export const PRIORITY_LEVELS = ["emergency", "high", "medium", "low"] as const;

export const DIRECTIONS = [
  "front",
  "back",
  "left",
  "right",
  "front_left",
  "front_right",
  "back_left",
  "back_right",
  "unknown",
] as const;

export const PLATFORMS = ["ios", "android"] as const;
export const CHANNELS = ["stable", "beta"] as const;

